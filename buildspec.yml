version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18  # Use a compatible Node.js version
    commands:
      - echo "Installing dependencies..."
      - npm install --legacy-peer-deps  # Install dependencies with Angular version compatibility

  build:
    commands:
      - echo "Building Angular application..."
      - npm run build --prod  # Build for production
      - echo "Zipping the build output..."
      - zip -r angular-build.zip dist/aqua-select/browser/*
      
post_build:
  commands:
    - echo "Verifying zip file before upload..."
    - ls -lh angular-build.zip  # List files to confirm zip exists
    - aws s3 ls s3://aquaflow195/  # List contents of your S3 bucket
    
artifacts:
  files:
    - angular-build.zip
  base-directory: .  # The location where the zip file is created
  discard-paths: yes
  
